---
phase: 01-workspace-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nvmrc
  - .node-version
  - .gitignore
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.json
  - .env.example
  - README.md
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-06
  - WORK-07
user_setup: []
must_haves:
  truths:
    - "D-06 is implemented: root `.env.example` documents `NEXT_PUBLIC_API_URL` and `NEXT_PUBLIC_USE_FIXTURES`."
    - "D-07 is implemented: root `README.md` contains install/dev/build commands and the app port map."
    - "D-13 is implemented: package manifests use current major ranges rather than `latest` dependency strings."
    - "D-14 is implemented: root `packageManager` uses an exact pnpm version, not `pnpm@latest`."
    - "D-15 is implemented: Node 24 is enforced with `.nvmrc`, `.node-version`, and `engines.node: \">=24\"`."
  artifacts:
    - .nvmrc
    - .node-version
    - .gitignore
    - package.json
    - pnpm-workspace.yaml
    - turbo.json
    - tsconfig.json
    - .env.example
    - README.md
  key_links:
    - package.json scripts invoke Turborepo for root build/dev/lint/type-check flows.
    - pnpm-workspace.yaml includes `apps/*` and `packages/*`.
    - tsconfig.json references all planned apps and shared packages.
---

<objective>
Create the root monorepo workspace contract for Riplai Web.

Purpose: Later phases need a stable pnpm/Turborepo root with pinned runtime, deterministic package manager metadata, scripts, workspace globs, TypeScript project references, environment documentation, and basic developer setup instructions.
Output: Root workspace files, Node version files, environment example, and README.
</objective>

<execution_context>
@/Users/agussuarya/Documents/agussuarya/repositories/riplai-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agussuarya/Documents/agussuarya/repositories/riplai-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-workspace-scaffold/01-CONTEXT.md
@.planning/phases/01-workspace-scaffold/01-RESEARCH.md
@SPEC.md
@docs/plans/2026-05-11-frontend-setup.md
</context>

<threat_model>
Threats considered for this scaffold plan:
- **Secret leakage:** `.env.example` must contain placeholder values only. Do not create `.env`, `.env.local`, tokens, API keys, or real domains beyond documented public defaults.
- **Supply-chain drift:** root `packageManager` must be exact and dependency specs must not use `latest`, reducing accidental unreviewed major upgrades.
- **Workspace misrouting:** workspace globs must be limited to `apps/*` and `packages/*` so unrelated local directories are not treated as packages.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pin runtime and ignore generated files</name>
  <files>.nvmrc, .node-version, .gitignore</files>
  <read_first>.planning/phases/01-workspace-scaffold/01-CONTEXT.md, SPEC.md, docs/plans/2026-05-11-frontend-setup.md</read_first>
  <action>Create `.nvmrc` containing exactly `24` and `.node-version` containing exactly `24`. Create `.gitignore` with these entries at minimum: `node_modules/`, `.next/`, `dist/`, `.turbo/`, `*.tsbuildinfo`, `.env*.local`, and `.DS_Store`. Do not ignore `.planning/` because git tracking is enabled.</action>
  <verify>test -f .nvmrc && test -f .node-version && rg '^24$' .nvmrc .node-version && rg 'node_modules/|\\.next/|dist/|\\.turbo/|\\*.tsbuildinfo|\\.env\\*\\.local|\\.DS_Store' .gitignore</verify>
  <acceptance_criteria>
    - `.nvmrc` contains exactly `24`.
    - `.node-version` contains exactly `24`.
    - `.gitignore` contains `node_modules/`, `.next/`, `dist/`, `.turbo/`, `*.tsbuildinfo`, `.env*.local`, and `.DS_Store`.
    - `.gitignore` does not contain `.planning/`.
  </acceptance_criteria>
  <done>Runtime pins and ignore rules exist exactly as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Create root package and Turborepo scripts</name>
  <files>package.json, turbo.json</files>
  <read_first>.planning/phases/01-workspace-scaffold/01-CONTEXT.md, .planning/phases/01-workspace-scaffold/01-RESEARCH.md, docs/plans/2026-05-11-frontend-setup.md</read_first>
  <action>Create root `package.json` with `name: "riplai-web"`, `private: true`, `engines.node: ">=24"`, `engines.pnpm: ">=9"`, and an exact `packageManager` value. Resolve the exact current pnpm version during implementation with `npm view pnpm version`; write the value as `pnpm@<version>` and do not write `pnpm@latest`. Add scripts: `build: "turbo build"`, `dev: "turbo dev"`, `lint: "turbo lint"`, `type-check: "turbo type-check"`, `dev:web: "turbo dev --filter=@riplai/web"`, `dev:partner: "turbo dev --filter=@riplai/partner"`, and `dev:admin: "turbo dev --filter=@riplai/admin"`. Add root devDependencies with major ranges, not latest: `turbo: "^2"` and `typescript: "^5"`. Create `turbo.json` with tasks `build`, `dev`, `lint`, and `type-check`; `dev` must be `cache: false` and `persistent: true`; `build` must depend on `^build` and output `.next/**`, `!.next/cache/**`, and `dist/**`; `type-check` must depend on `^build`.</action>
  <verify>node -e "const p=require('./package.json'); if(p.packageManager==='pnpm@latest'||!/^pnpm@\\d+\\.\\d+\\.\\d+/.test(p.packageManager)) throw new Error('bad packageManager'); for (const k of ['build','dev','lint','type-check','dev:web','dev:partner','dev:admin']) if(!p.scripts[k]) throw new Error('missing '+k); if(p.engines.node!=='>=24') throw new Error('bad node engine'); if(p.devDependencies.turbo==='latest'||p.devDependencies.typescript==='latest') throw new Error('latest dependency');" && node -e "const t=require('./turbo.json'); for (const k of ['build','dev','lint','type-check']) if(!t.tasks[k]) throw new Error('missing task '+k);"</verify>
  <acceptance_criteria>
    - `package.json` has `name` equal to `riplai-web` and `private` equal to `true`.
    - `package.json` has `engines.node` equal to `>=24`.
    - `package.json` has `packageManager` matching `pnpm@<exact semver>`, not `pnpm@latest`.
    - `package.json` scripts include `build`, `dev`, `lint`, `type-check`, `dev:web`, `dev:partner`, and `dev:admin`.
    - `package.json` devDependencies use `^2` for `turbo` and `^5` for `typescript`; neither value is `latest`.
    - `turbo.json` defines tasks `build`, `dev`, `lint`, and `type-check`.
  </acceptance_criteria>
  <done>Root package metadata and Turborepo task graph are present and parseable.</done>
</task>

<task type="auto">
  <name>Task 3: Define workspace and TypeScript references</name>
  <files>pnpm-workspace.yaml, tsconfig.json</files>
  <read_first>docs/plans/2026-05-11-frontend-setup.md, .planning/phases/01-workspace-scaffold/01-CONTEXT.md</read_first>
  <action>Create `pnpm-workspace.yaml` with exactly two workspace globs: `apps/*` and `packages/*`. Create root `tsconfig.json` with `"files": []` and `"references"` entries for `packages/config`, `packages/types`, `packages/utils`, `packages/api-client`, `packages/ui`, `apps/web`, `apps/partner`, and `apps/admin`. Use `apps/partner`, not `apps/merchant`.</action>
  <verify>rg 'apps/\\*|packages/\\*' pnpm-workspace.yaml && node -e "const t=require('./tsconfig.json'); const refs=t.references.map(r=>r.path).sort(); const expected=['apps/admin','apps/partner','apps/web','packages/api-client','packages/config','packages/types','packages/ui','packages/utils'].sort(); if(JSON.stringify(refs)!==JSON.stringify(expected)) throw new Error(refs.join(','));" && ! rg 'apps/merchant|@riplai/merchant|MerchantUsage|\\bMerchant\\b' pnpm-workspace.yaml tsconfig.json</verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` includes `apps/*`.
    - `pnpm-workspace.yaml` includes `packages/*`.
    - Root `tsconfig.json` references all three apps and all five shared packages.
    - Root workspace files do not reference `apps/merchant`, `@riplai/merchant`, `Merchant`, or `MerchantUsage`.
  </acceptance_criteria>
  <done>Workspace globs and TypeScript project references match the partner-only scaffold.</done>
</task>

<task type="auto">
  <name>Task 4: Add environment and setup documentation</name>
  <files>.env.example, README.md</files>
  <read_first>.planning/phases/01-workspace-scaffold/01-CONTEXT.md, SPEC.md</read_first>
  <action>Create root `.env.example` with `NEXT_PUBLIC_API_URL=http://localhost:8080` and `NEXT_PUBLIC_USE_FIXTURES=true`. Create a concise root `README.md` with sections for prerequisites, install, development commands, build/type-check commands, and the port map. The port map must list `apps/web` on `3001`, `apps/partner` on `3002`, `apps/admin` on `3003`, and API on `8080`. Keep README setup-focused; do not add architecture essays or rewrite historical plan docs.</action>
  <verify>rg '^NEXT_PUBLIC_API_URL=http://localhost:8080$|^NEXT_PUBLIC_USE_FIXTURES=true$' .env.example && rg 'pnpm install|pnpm dev:web|pnpm dev:partner|pnpm dev:admin|pnpm build|pnpm type-check|3001|3002|3003|8080' README.md</verify>
  <acceptance_criteria>
    - `.env.example` contains `NEXT_PUBLIC_API_URL=http://localhost:8080`.
    - `.env.example` contains `NEXT_PUBLIC_USE_FIXTURES=true`.
    - `README.md` contains `pnpm install`.
    - `README.md` contains `pnpm dev:web`, `pnpm dev:partner`, and `pnpm dev:admin`.
    - `README.md` contains `pnpm build` and `pnpm type-check`.
    - `README.md` lists ports `3001`, `3002`, `3003`, and `8080`.
  </acceptance_criteria>
  <done>Root environment example and basic README are sufficient for a developer to install and run the scaffold.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node -e "require('./package.json'); require('./turbo.json'); require('./tsconfig.json')"` exits 0.
- [ ] `rg 'pnpm@latest|\"latest\"' package.json` returns no matches.
- [ ] `rg 'apps/merchant|@riplai/merchant|MerchantUsage|\\bMerchant\\b' package.json pnpm-workspace.yaml tsconfig.json README.md .env.example` returns no matches.
- [ ] `.nvmrc`, `.node-version`, `.env.example`, and `README.md` contain the exact values required above.
</verification>

<success_criteria>

- Root workspace files exist and are parseable.
- Root scripts expose all required build/dev/lint/type-check commands.
- Workspace globs and TypeScript references use partner-only app naming.
- Environment and README documentation match the agreed scaffold scope.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-scaffold/01-01-SUMMARY.md`
</output>
